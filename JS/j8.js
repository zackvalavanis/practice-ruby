// Write a program that uses variables to store a first and last name, then prints the full name in one line using string concatenation (the + operator).

var first_name = 'zack'
var last_name = "valavanis"

var  both = first_name + " " + last_name
console.log(both)

